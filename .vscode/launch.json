{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/push_swap",
            "args": ["3", "0", "1", "4", "2"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false
        }
    ]
}